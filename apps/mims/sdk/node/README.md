# @pharaxis/mims-node

```js
import { MimsClient } from '@pharaxis/mims-node'
const mims = new MimsClient({ baseUrl: 'https://mims.example.com', clientId, clientSecret })
const cases = await mims.cases({ limit: 25 })
```
