# QMS Frontend Modules

This folder is the MIMS-style target home for QMS feature modules.

Current Vue views remain in `src/views` to avoid a risky one-shot route migration. Future moves should happen module by module and keep `src/shared/*` for API, session, route guards, and common UI utilities.
