name: AI-Agent Remote Deploy Disabled

on:
  workflow_dispatch:

jobs:
  local-only-status:
    uses: ./.github/workflows/_app-deploy.yml
    secrets: inherit
    with:
      app_name: AI-Agent
      app_slug: ai-agent
      app_root: apps/ai-agent
      frontend_dir: apps/ai-agent/frontend
      frontend_lockfile: apps/ai-agent/frontend/package-lock.json
      publish_dir: local-only-disabled
      pm2_name: local-only-disabled
      environment_name: local-only-disabled
