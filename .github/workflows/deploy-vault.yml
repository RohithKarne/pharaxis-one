name: Vault Remote Deploy Disabled

on:
  workflow_dispatch:

jobs:
  local-only-status:
    uses: ./.github/workflows/_app-deploy.yml
    secrets: inherit
    with:
      app_name: Vault
      app_slug: vault
      app_root: apps/vault
      frontend_dir: apps/vault/frontend
      frontend_lockfile: apps/vault/frontend/package-lock.json
      publish_dir: local-only-disabled
      pm2_name: local-only-disabled
      environment_name: local-only-disabled
