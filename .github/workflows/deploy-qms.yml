name: QMS Remote Deploy Disabled

on:
  workflow_dispatch:

jobs:
  local-only-status:
    uses: ./.github/workflows/_app-deploy.yml
    secrets: inherit
    with:
      app_name: QMS
      app_slug: qms
      app_root: apps/qms
      frontend_dir: apps/qms/frontend
      frontend_lockfile: apps/qms/frontend/package-lock.json
      publish_dir: local-only-disabled
      pm2_name: local-only-disabled
      environment_name: local-only-disabled
