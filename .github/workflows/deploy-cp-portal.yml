name: CP Portal Remote Deploy Disabled

on:
  workflow_dispatch:

jobs:
  local-only-status:
    uses: ./.github/workflows/_app-deploy.yml
    secrets: inherit
    with:
      app_name: CP Portal
      app_slug: cp-portal
      app_root: apps/cp-portal
      frontend_dir: apps/cp-portal/frontend
      frontend_lockfile: apps/cp-portal/frontend/package-lock.json
      publish_dir: local-only-disabled
      pm2_name: local-only-disabled
      environment_name: local-only-disabled
